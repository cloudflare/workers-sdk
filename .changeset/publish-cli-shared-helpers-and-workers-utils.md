---
"@cloudflare/cli-shared-helpers": minor
"@cloudflare/workers-utils": minor
---

Prepare `@cloudflare/cli-shared-helpers` and `@cloudflare/workers-utils` for npm publication

The internal `@cloudflare/cli` package has been renamed to `@cloudflare/cli-shared-helpers`, given a tsdown build pipeline, and marked as public so it can be published to npm. `@cloudflare/workers-utils` has also been marked as public. Both packages are now available for external consumers.
