---
"create-cloudflare": patch
---

Add Local Explorer API documentation to generated AGENTS.md

The AGENTS.md file generated by `create-cloudflare` now includes a "Local Explorer (Debugging & Inspection)" section documenting the API endpoints available during `wrangler dev`. This helps AI coding agents discover and use the Local Explorer to inspect local Workers, bindings, storage state (KV, D1, R2, Durable Objects), and query request traces and logs.
