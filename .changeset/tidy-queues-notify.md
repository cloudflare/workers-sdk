---
"wrangler": minor
"@cloudflare/deploy-helpers": minor
---

Add Queue notification consumer commands

Wrangler can now add, list, and remove Queue notification consumers configured with email, PagerDuty, or webhook destinations. The supporting deploy helpers expose notification consumer settings and deletion behavior. Queue notification consumers are currently in beta.
