---
"miniflare": major
---

Remove `ModuleRuleSchema` and `CompiledModuleRule` from Miniflare's public API

Modules must now be provided inline via the config manifest, so module rules have been removed.
