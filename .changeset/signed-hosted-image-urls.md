---
"miniflare": minor
---

Add local simulation for signed Hosted Images URLs

Miniflare's Images binding now supports `env.IMAGES.hosted.image(id).signedUrl()` locally. Private hosted image delivery rejects unsigned requests and accepts URLs generated by `signedUrl()`, matching the production API shape for local development.
