---
"wrangler": minor
---

Add experimental APIs for querying Workers AI model schemas and generated model types

Wrangler now exposes experimental programmatic helpers for listing Workers AI models, fetching model schemas, and generating TypeScript declarations from those schemas. These APIs are intended for editor and tooling integrations that need current model types from the public Cloudflare API.
