---
"wrangler": patch
---

Explain how to provision draft Flagship bindings during remote development

Wrangler now reports that a Flagship binding without an `app_id` must first be provisioned with `wrangler deploy` or configured with an existing app ID.
