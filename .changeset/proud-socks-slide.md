---
"@cloudflare/vite-plugin": patch
---

Fix: Ensure that `vite dev` and `vite preview` crash with an appropriate error message a remote proxy session is required but it the connection with it fails to be established
