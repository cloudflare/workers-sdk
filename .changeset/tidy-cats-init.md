---
"@cloudflare/autoconfig": minor
---

Initialize empty directories as Vite Workers for the `cf` target

New projects can now use the existing autoconfig flow to create a minimal Worker, install Vite and the Cloudflare Vite plugin, and generate the required configuration.
