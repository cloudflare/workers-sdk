---
"wrangler": minor
---

Improve beta Preview configuration onboarding

Wrangler now builds suggested Preview configuration from shared binding metadata, retains safe settings, and recursively redacts production string values that need Preview-specific replacements.
