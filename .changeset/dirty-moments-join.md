---
"wrangler": minor
---

Re-export `supportedCompatibilityDate` from miniflare

Re-exports the `supportedCompatibilityDate` value form miniflare that contains a recent and safe compatibility date from the local version of `workerd`
