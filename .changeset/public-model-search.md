---
"wrangler": minor
---

Add filters to `wrangler ai models` for querying the Workers AI model catalog

`wrangler ai models` now accepts `--search`, `--task`, `--author`, `--source`, and `--hide-experimental`, matching the public model catalog search endpoint.
