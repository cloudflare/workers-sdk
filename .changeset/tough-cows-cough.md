---
"wrangler": patch
---

Ensure that the current interactive behavior for `wrangler deploy <directory>` and `wrangler deploy --assets <directory>` is maintained even if `--experimental-autoconfig` is specified
