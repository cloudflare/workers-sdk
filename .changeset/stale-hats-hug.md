---
"wrangler": patch
---

Fix: ensure that when a remote proxy session (for remote bindings) cannot be established an appropriate error is surfaced to the user (both in `wrangler dev` and in the programmatic API)
