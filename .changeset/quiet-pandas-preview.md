---
"wrangler": patch
---

`[private beta]`: Explain how to enable Preview URLs when a Preview deployment has none

Wrangler now shows the expected Workers.dev and custom-domain URL shapes, names the relevant `preview_urls` and `previews_enabled` configuration fields, and warns that deploying those settings also publishes the code in the current checkout.
