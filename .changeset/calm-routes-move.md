---
"@cloudflare/vite-plugin": major
---

Use Miniflare v5 local testing paths

Vite plugin v2 now forwards the canonical `/cdn-cgi/local/*` and `/__cf_local/*` endpoints directly and no longer rewrites Miniflare v4 paths.
