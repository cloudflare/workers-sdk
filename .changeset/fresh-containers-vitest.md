---
"@cloudflare/vitest-plugin": minor
---

Add support for testing container-backed Durable Objects with the Workers runtime.

The plugin now prepares scheduler-backed default images and Durable Object-managed named images from the Worker configuration. Cloudflare-managed registry images are not supported yet.
