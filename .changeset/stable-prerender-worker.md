---
"@cloudflare/vite-plugin": minor
---

Stabilize the `prerenderWorker` plugin option

The dedicated prerender Worker is now configured with the top-level `prerenderWorker` option instead of `experimental.prerenderWorker`.
