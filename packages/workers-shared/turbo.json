{
	"$schema": "http://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": [
				"asset-worker/**",
				"router-worker/**",
				"utils/**",
				"*.js",
				"*.ts",
				"*.json"
			],
			"outputs": ["dist/**"]
		}
	}
}
