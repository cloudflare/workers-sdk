# `@cloudflare/runtime-types`

Utilities for generating Cloudflare Workers runtime types from compatibility dates and flags.

This is not yet stable enough for external use — APIs may change without notice.
