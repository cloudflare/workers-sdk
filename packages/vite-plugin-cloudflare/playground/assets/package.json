{
	"name": "@playground/assets",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"build:no-client": "vite build -c vite.config.no-client.ts",
		"build:public-only": "vite build -c vite.config.public-only.ts",
		"check:types": "tsc --build",
		"dev": "vite dev",
		"dev:no-client": "vite dev -c vite.config.no-client.ts",
		"dev:public-only": "vite dev -c vite.config.public-only.ts",
		"preview": "vite preview"
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "workspace:*",
		"@cloudflare/workers-tsconfig": "workspace:*",
		"@cloudflare/workers-types": "^4.20250604.0",
		"typescript": "catalog:default",
		"vite": "catalog:vite-plugin",
		"wrangler": "workspace:*"
	}
}
