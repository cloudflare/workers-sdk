{
	"$schema": "http://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"inputs": [
				"asset-worker/**",
				"router-worker/**",
				"utils/**",
				"*.js",
				"*.ts",
				"*.json"
			],
			"outputs": ["dist/**"]
		}
	}
}
