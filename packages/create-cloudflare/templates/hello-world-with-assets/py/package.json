{
  "name": "<PACKAGE_NAME>",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy",
    "dev": "pnpm -w enforce-turbo && uv run pywrangler dev",
    "start": "uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.101.0"
  }
}
