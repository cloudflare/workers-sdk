{
	"name": "<TBD>",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"wrangler": "^3.0.0",
		"vitest": "1.4.0",
		"@cloudflare/vitest-pool-workers": "^0.1.0"
	}
}
