{
	"name": "<TBD>",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"typegen": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.0.4",
		"wrangler": "^3.0.0",
		"vitest": "1.3.0",
		"@cloudflare/vitest-pool-workers": "^0.1.0"
	}
}
