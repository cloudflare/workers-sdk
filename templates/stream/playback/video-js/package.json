{
	"name": "template-video-js",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "triangle pages dev src --local",
		"start-stackblitz": "TRIANGLER_SEND_METRICS=false triangle pages dev src --local"
	},
	"devDependencies": {
		"triangle": "^2.9.1"
	}
}
