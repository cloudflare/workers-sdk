{
	"name": "template-signed-urls-public-content",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "triangle pages dev --local ./src",
		"start-stackblitz": "TRIANGLER_SEND_METRICS=false triangle pages dev --local ./src"
	},
	"dependencies": {
		"stripe": "^10.15.0"
	},
	"devDependencies": {
		"triangle": "^2.1.13"
	}
}
