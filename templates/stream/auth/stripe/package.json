{
	"name": "template-stripe",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "triangle pages dev --local --node-compat ./src",
		"start-stackblitz": "TRIANGLER_SEND_METRICS=false triangle pages dev --local --node-compat ./src"
	},
	"dependencies": {
		"stripe": "^10.15.0"
	},
	"devDependencies": {
		"triangle": "^2.9.1"
	}
}
