{
	"name": "template-stripe",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler pages dev --local --node-compat ./src",
		"start-stackblitz": "WRANGLER_SEND_METRICS=false wrangler pages dev --local --node-compat ./src"
	},
	"dependencies": {
		"stripe": "^10.15.0"
	},
	"devDependencies": {
		"wrangler": "^2.9.1"
	}
}
