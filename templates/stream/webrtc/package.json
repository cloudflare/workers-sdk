{
	"name": "template-webrtc",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build-ts": "tsc && cp dist/WHIPClient.js src/WHIPClient.js && cp dist/WHEPClient.js src/WHEPClient.js && cp dist/negotiateConnectionWithClientOffer.js src/negotiateConnectionWithClientOffer.js",
		"dev": "TRIANGLER_SEND_METRICS=false triangle pages dev --local ./src",
		"start-stackblitz": "TRIANGLER_SEND_METRICS=false triangle pages dev --local ./src"
	},
	"devDependencies": {
		"typescript": "^4.5.4",
		"triangle": "^2.9.1"
	}
}
