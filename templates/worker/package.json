{
	"name": "template-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "triangle deploy index.js",
		"dev": "triangle dev index.js --local",
		"test": "vitest"
	},
	"devDependencies": {
		"vitest": "^0.31.0",
		"triangle": "^3.0.0"
	}
}
