{
	"name": "template-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy index.js",
		"dev": "wrangler dev index.js --local",
		"test": "vitest"
	},
	"devDependencies": {
		"vitest": "^0.32.2",
		"wrangler": "^3.0.0"
	}
}
