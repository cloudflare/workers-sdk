{
  "name": "@fixture/pages-workerjs",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "check:type": "pnpm -w enforce-turbo && tsc",
    "dev": "pnpm -w enforce-turbo && wrangler pages dev ./workerjs-test --port 8792 --compatibility-date=2025-07-15",
    "test:ci": "pnpm -w enforce-turbo && vitest run",
    "test:watch": "pnpm -w enforce-turbo && vitest",
    "type:tests": "tsc -p ./tests/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-tsconfig": "workspace:^",
    "typescript": "catalog:default",
    "undici": "catalog:default",
    "vitest": "catalog:default",
    "wrangler": "workspace:*"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
