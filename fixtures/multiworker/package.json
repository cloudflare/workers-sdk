{
	"name": "multiworker",
	"private": true,
	"description": "",
	"license": "ISC",
	"author": "",
	"main": "src/index.js",
	"scripts": {
		"deploy": "pnpm wrangler deploy -c worker-a/wrangler.toml",
		"dev": "pnpm wrangler dev -c worker-a/wrangler.toml"
	},
	"dependencies": {
		"cookie": "^0.6.0",
		"isomorphic-random-example": "workspace:^"
	},
	"devDependencies": {
		"@cloudflare/workers-tsconfig": "workspace:^",
		"typescript": "catalog:default",
		"undici": "catalog:default",
		"vitest": "catalog:default",
		"wrangler": "workspace:*"
	},
	"volta": {
		"extends": "../../package.json"
	}
}
