{
	"name": "type-generation-fixture",
	"private": true,
	"description": "A test for the `wrangler types` utility",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"wrangler": "workspace:*"
	},
	"volta": {
		"extends": "../../package.json"
	}
}
