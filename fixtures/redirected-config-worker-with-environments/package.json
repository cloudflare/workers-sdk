{
  "name": "@fixture/config-redirected-envs",
  "private": true,
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "src/index.js",
  "scripts": {
    "build": "pnpm -w enforce-turbo && node -r esbuild-register tools/build.ts",
    "check:type": "pnpm -w enforce-turbo && tsc",
    "dev": "pnpm -w enforce-turbo && pnpm run build && wrangler dev",
    "test:ci": "pnpm -w enforce-turbo && pnpm run build && vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-tsconfig": "workspace:^",
    "undici": "catalog:default",
    "vitest": "catalog:default",
    "wrangler": "workspace:*"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
