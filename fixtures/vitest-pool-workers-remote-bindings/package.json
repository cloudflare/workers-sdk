{
  "name": "@fixture/vitest-pool-workers-remote-bindings",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -w enforce-turbo && vitest run --config vitest.workers.config.ts",
    "test:ci": "pnpm -w enforce-turbo && node run-tests.mjs",
    "test:vitest": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "workspace:*",
    "typescript": "catalog:default",
    "vitest": "catalog:default",
    "wrangler": "workspace:*"
  }
}
