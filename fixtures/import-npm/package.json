{
  "name": "@fixture/import-npm",
  "private": true,
  "description": "",
  "author": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "_clean_install": "rm -rf node_modules && npm install --no-package-lock",
    "check:type": "pnpm -w enforce-turbo && npm run check:type --workspaces",
    "test:ci": "pnpm -w enforce-turbo && npm run test:ci --workspaces",
    "test:watch": "pnpm -w enforce-turbo && npm run test:watch --workspaces",
    "type:tests": "npm run type:tests --workspaces"
  }
}
