{
	"name": "node-types-test",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"test": "npx wrangler types --x-include-runtime && vitest",
		"test:ci": "npm run test"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"vitest": "1.5.0",
		"wrangler": "^3.60.3"
	},
	"dependencies": {
		"@types/node": "^22.5.5"
	}
}
