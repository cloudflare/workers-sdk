{
	"name": "fixtures-shared",
	"version": "0.0.1",
	"private": true,
	"description": "Shared fixtures for testing",
	"devDependencies": {
		"wrangler": "workspace:*"
	},
	"volta": {
		"extends": "../../package.json"
	}
}
