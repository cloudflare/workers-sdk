{
	"name": "no-bundle-import",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"start": "wrangler dev",
		"test": "vitest run",
		"test:ci": "vitest run",
		"test:watch": "npx vitest"
	},
	"devDependencies": {
		"get-port": "^6.1.2",
		"wrangler": "workspace:*"
	}
}
